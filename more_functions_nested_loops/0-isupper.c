#include "main.h"

/**
 * _isupper - check if lowercase or uppercase
 * @c: letter
 * Return: 1 if uppercase 0 if else
 */


int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
